

- Port remaining data classes
- Graceful depency fallbacks using LazyLoad


# On the horizon:

- Shift::Array which delegates methods to its members
- Tilt coverage
- A MethodCompiler class or Gem to do what Tilt does.

RubySource: evaluate, safe_eval, compile
CompiledRuby: call(binding, locals)
RubyTemplate: make_portable

